<% include ../../partials/head %>
<script>
function checkPublicHolidays(x)
{
	
}

$( document ).ready(function() {
  //Get Public Holidays List
  $.get("\staff\public", function(data, status){
        alert("Data: " + data + "\nStatus: " + status);
    });

  //From Date to To Date Change
  $("#fromDate").on('change',function(){
  		var fromDate = new Date($("#fromDate").val());
		$("#toDate").val(new Date(fromDate.setDate(fromDate.getDate()+<%=apply.nod%>)));
	});
});
</script>
<div class="fontlg">
	<div class="container-fluid">
		<br/>
		<h3> Date Selection</h3>
		<hr/>
		<form method='POST' action='/staff/alterations' class="col-lg-9">
			<div class="form-group row">
				<label for="fromDate" class="col-sm-2 col-form-label"> Date </label>
				<div class="col-sm-4">
      				<input type="date"  class="form-control" id="fromDate" >
    			</div>
    			<label for="to" class="col-sm-2 col-form-label"> to </label>
    			<div class="col-sm-4">
      				<input type="text"  class="form-control" id="toDate" readonly>
    			</div>
			</div>
			<div class="form-group row">
				<label for="reason" class="col-sm-2 col-form-label"> Reason </label>
				<div class="col-sm-10">
      				<input type="text" class="form-control" id="reason" >
    			</div>
			</div>
			<div class="form-group row">
				<div class="col-sm-4 offset-sm-4">
      				<input type="submit" class="btn btn-block btn-success" value="Proceed to Alteration">
    			</div>
			</div>
		</form>
	</div>
</div>
<% include ../../partials/footer %>